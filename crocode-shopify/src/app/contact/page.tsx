import { ContactPage } from "@/components/pages"
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact Us | Crocode",
  description: "Get in touch with Crocode's Shopify experts to discuss your project, request a quote, or schedule a consultation.",
}

export default () => <ContactPage/>
